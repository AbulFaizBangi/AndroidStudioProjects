
// settings.gradle.kts
pluginManagement {
    repositories {
        google() // Google repository for Android libraries
        mavenCentral() // Maven Central repository
    }
}

rootProject.name = "ACOrderTrackingApp" // Replace with your project name
